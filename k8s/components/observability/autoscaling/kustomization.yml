apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
- pod-disruption-budget.yml
- horizontal-pod-autoscaler.yml

# This component provides autoscaling and availability management
# Including PodDisruptionBudget for safe rollouts
# and HorizontalPodAutoscaler for load-based scaling