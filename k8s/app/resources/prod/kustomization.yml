apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Prod profile - production-grade resources and high availability

resources:
- ../../base
- ../../../namespace/namespace.yml
- horizontal-pod-autoscaler.yml

patches:
- target:
    kind: Deployment
    labelSelector: "app=webapp"
  patch: |-
    - op: replace
      path: /spec/replicas
      value: 3
    - op: replace
      path: /spec/template/spec/containers/0/resources
      value:
        requests:
          memory: "512Mi"
          cpu: "500m"
        limits:
          memory: "1Gi"
          cpu: "1000m"
    - op: add
      path: /spec/template/spec/affinity
      value:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - webapp
              topologyKey: kubernetes.io/hostname
    - op: replace
      path: /spec/template/spec/initContainers/0/args
      value:
      - "projects/u2i-tenant-webapp-prod/locations/europe-west1/clusters/webapp-prod-alloydb/instances/webapp-prod-primary"
      - "--port=5432"
      - "--auto-iam-authn"
- target:
    kind: Job
    name: webapp-migration
  patch: |-
    - op: replace
      path: /spec/template/spec/initContainers/0/args
      value:
      - "projects/u2i-tenant-webapp-prod/locations/europe-west1/clusters/webapp-prod-alloydb/instances/webapp-prod-primary"
      - "--port=5432"
      - "--max-connections=10"
      - "--auto-iam-authn"
    - op: replace
      path: /spec/template/spec/containers/0/image
      value: europe-west1-docker.pkg.dev/u2i-tenant-webapp-prod/webapp-images/webapp

configMapGenerator:
- name: webapp-profile-config
  literals:
    - PROFILE=prod
    - LOG_LEVEL=info
    - GRACEFUL_SHUTDOWN_TIMEOUT=30s
    - ENABLE_PROFILING=false
    - ENABLE_METRICS=true