# Secret Manager integration for preview environments
# Using native GKE Secret Manager CSI driver with workload identity
apiVersion: v1
kind: Secret
metadata:
  name: webapp-demo-secret
  annotations:
    gke.io/secret-manager-ref: "webapp-demo-secret"
type: Opaque