apiVersion: v1
kind: ResourceQuota
metadata:
  name: webapp-team-quota
  namespace: webapp-team
spec:
  hard:
    requests.cpu: "2"
    requests.memory: "4Gi"
    limits.cpu: "4"
    limits.memory: "8Gi"
    pods: "10"