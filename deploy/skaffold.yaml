# GENERATED FILE - DO NOT EDIT MANUALLY
# This file is auto-generated by compliance-cli
# Source: internal/generate/templates/skaffold.yaml.tmpl
# To make changes, edit .compliance-cli.yml and run: compliance-cli generate
#
# Unified Skaffold configuration for all environments
# Includes namespace, application, and certificates for all deployments
# Uses tolerateFailuresUntilDeadline to prevent certificate timeout failures

apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: webapp
build:
  artifacts:
  - image: europe-west1-docker.pkg.dev/u2i-tenant-webapp-nonprod/webapp-images/webapp
    context: .
    docker:
      dockerfile: Dockerfile
  tagPolicy:
    gitCommit: {}
  local:
    concurrency: 1
deploy:
  kubectl:
    flags:
      apply: ["--server-side", "--force-conflicts"]
  statusCheck: true
profiles:
# Preview profile - disables status checking to avoid Config Connector bug
- name: preview-all
  manifests:
    kustomize:
      paths:
      - ../k8s/app/resources/preview
      buildArgs:
      - --load-restrictor=LoadRestrictionsNone
  deploy:
    kubectl:
      flags:
        apply: ["--server-side", "--force-conflicts"]
    statusCheck: false  # Disabled due to Skaffold Config Connector bug #7207

# Dev profile
- name: dev
  manifests:
    kustomize:
      paths: 
      - ../k8s/app/resources/nonprod
      buildArgs:
      - --load-restrictor=LoadRestrictionsNone

# QA profile
- name: qa
  manifests:
    kustomize:
      paths: 
      - ../k8s/app/resources/nonprod
      buildArgs:
      - --load-restrictor=LoadRestrictionsNone

# Production profile with prod image registry
- name: prod
  build:
    artifacts:
    - image: europe-west1-docker.pkg.dev/u2i-tenant-webapp-prod/webapp-images/webapp
      context: .
      docker:
        dockerfile: Dockerfile
  manifests:
    kustomize:
      paths: 
      - ../k8s/app/resources/prod
      buildArgs:
      - --load-restrictor=LoadRestrictionsNone