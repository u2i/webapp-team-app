# Unified Skaffold configuration for all environments
# Includes namespace, application, and certificates for all deployments
# Uses tolerateFailuresUntilDeadline to prevent certificate timeout failures

apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: webapp
build:
  artifacts:
  - image: europe-west1-docker.pkg.dev/u2i-tenant-webapp-nonprod/webapp-images/webapp
    context: .
    docker:
      dockerfile: Dockerfile
  tagPolicy:
    gitCommit: {}
  local:
    concurrency: 1
deploy:
  kubectl:
    flags:
      apply: ["--server-side", "--force-conflicts"]
  statusCheck: true
profiles:
# Preview profile - includes all resources with tolerant status checking
- name: preview-all
  manifests:
    rawYaml:
    - ../k8s/namespace/namespace.yml
    - ../k8s/gcp/certificate.yml
    kustomize:
      paths:
      - ../k8s/app/resources/preview
      buildArgs:
      - --load-restrictor=LoadRestrictionsNone
  deploy:
    kubectl:
      flags:
        apply: ["--server-side", "--force-conflicts"]
    statusCheck: false  # Disabled due to Skaffold bug with cross-namespace Config Connector resources

# Dev profile - includes namespace creation
- name: dev
  manifests:
    rawYaml:
    - ../k8s/namespace/namespace.yml
    kustomize:
      paths: 
      - ../k8s/app/resources/preprod
      buildArgs:
      - --load-restrictor=LoadRestrictionsNone

# QA profile - includes namespace creation
- name: qa
  manifests:
    rawYaml:
    - ../k8s/namespace/namespace.yml
    kustomize:
      paths: 
      - ../k8s/app/resources/preprod
      buildArgs:
      - --load-restrictor=LoadRestrictionsNone

# Production profile - includes namespace creation with prod image registry
- name: prod
  build:
    artifacts:
    - image: europe-west1-docker.pkg.dev/u2i-tenant-webapp-prod/webapp-images/webapp
      context: .
      docker:
        dockerfile: Dockerfile
  manifests:
    rawYaml:
    - ../k8s/namespace/namespace.yml
    kustomize:
      paths: 
      - ../k8s/app/resources/prod
      buildArgs:
      - --load-restrictor=LoadRestrictionsNone